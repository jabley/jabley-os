Making an OS by following http://intermezzos.github.io/

# Getting Started

```
$ vagrant up
$ vagrant ssh
> pushd /vagrant
> make clean
> make
```

This will run the OS, but you may need to start another shell to kill the qemu process.
